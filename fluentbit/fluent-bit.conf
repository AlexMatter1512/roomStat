[SERVICE]
    FlushInterval 1s
    LogLevel      debug
    ParsersFile   /etc/parsers.conf

[INPUT]
    Name   tail
    Tag    data
    Path   /var/log/macs.log
    Parser json
    # Parser csv

[OUTPUT]
    Name   stdout
    format json_lines
    Match  *